# Copy this file to .env and fill in your local paths.
# .env is gitignored — never commit credentials or personal paths.

# Path to the directory containing raw macro data parquet files.
# Overrides configs/data/regime_universe.yaml > regime_universe.data_path
MACRO_DATA_PATH=/path/to/your/quant-data/macro
